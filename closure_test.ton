fn make_counter() {
    let i = 0;
    fn count() {
        i = i + 1;
        return i;
    }
    return count;
}

let counter = make_counter();

print(counter());
print(counter());
print(counter());