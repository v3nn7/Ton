// Test makra z parametrami
macro add(a, b) {
    return a + b;
}

macro greet(name) {
    return "Hello, " + name + "!";
}

print "Testowanie makr z parametrami";
let sum = add(5, 3);
print sum;

let greeting = greet("World");
print greeting;